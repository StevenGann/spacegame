<?xml version="1.0" encoding="utf-8"?>
<spaceship base="ship\base_fighter">
  <texture>ship\barb</texture>
  <tick>
    void Tick(double Delta, SpaceGame.SpaceShip Entity)
    { 
      float flag = (float)SpaceGame.Debug.GetFlag("grow"); 
      if(flag != 0){ Entity.Scale += flag; } 
    }
  </tick>
  <maxthrust>**2.0</maxthrust>
</spaceship>